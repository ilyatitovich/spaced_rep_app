/**
 * Default e2e fixtures. Specs import `{ test, expect }` from here.
 * Swap to auth/webauthn fixture modules when writing those suites.
 */
export { test, expect } from '../testing/fixtures/base.fixture.js'
